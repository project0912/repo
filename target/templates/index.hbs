<div class="container-fluid">
    <div class="row-fluid">
        <div class="span12">
            {{view App.CrisesMapView class="hg352"}}
        </div>
    </div>
</div>
    <div class="container-fluid spacingFooter">
        <div class="row-fluid">
            <div class="span6">
                <div class="navbar">
                    <div class="navbar-inner">
                        <a class="brand" href="#">Top</a> </div>
                    </div>
                <ul class="nav nav-tabs nav-stacked">
                        {{#each claim in top}}
                            <li>
                            {{#link-to "crisis" claim}}
                                {{claim.title}}
                            {{/link-to}}
                            </li>
                	{{/each}}
                 </ul>
            </div>
            <div class="span6">
                <div class="navbar">
                    <div class="navbar-inner">
                        <a class="brand" href="#">Recent</a>
                    </div>
                </div>
                <ul class="nav nav-tabs nav-stacked">
                    {{#each claim in recent}}
                        <li>
                            {{#link-to "crisis" claim}}
                                {{claim.title}}
                            {{/link-to}}
                        </li>
                    {{/each}}
                </ul>
            </div>
        </div>
    </div>