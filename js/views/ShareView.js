define(['ember'],
        function(Ember) {
            "use strict";

            var ShareView = Ember.View.extend({
                templateName: 'user/share'
            });

            return ShareView;
        });